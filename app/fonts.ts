import { Orbitron, Exo_2 } from 'next/font/google';

export const orbitron = Orbitron({
  subsets: ['latin'], //which alphabet to support
  variable: '--font-orbitron'
});

export const exo_2 = Exo_2({
  subsets: ['latin'], //which alphabet to support
  variable: '--font-exo-2'
});